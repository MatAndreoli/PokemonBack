pipeline {
    agent any
    tools {
        maven 'Maven363' 
    }
    environment {
        DATE = new Date().format('yy.M')
        TAG = "${DATE}.${BUILD_NUMBER}"
        DOCKERFILE_PATH = "src/main/docker"
    }
    stages {
        stage('Update Version') {
            steps {
                echo "Updating app version..."
                sh '''
                git remote set-url origin https://github.com/MatAndreoli/PokemonBack.git
                python3 update_version.py
                git add .
                git commit -m 'Jenkins Update Version'
                git push origin Main
                '''
                gitPublisher branchesToPush: [[branchName: 'Main']], credentialsId: 'GitHubCreds', url: 'https://github.com/MatAndreoli/PokemonBack.git'
            }
        }
    }
}
